# Next.js Server & Client Components Demo

Демонстраційний проєкт, який показує правильне використання Server Components та Client Components в Next.js 14+ з App Router.

## 📋 Опис проєкту

Цей проєкт демонструє архітектуру Next.js з розмежуванням між Server Components та Client Components на прикладі каталогу продуктів з функціоналом додавання до кошика та обраних.

## 🏗️ Архітектура проєкту

### Структура файлів

```
app/
├── page.tsx                    # Server Component - головна сторінка
├── layout.tsx                  # Root layout
├── globals.css                 # Глобальні стилі
├── types/
│   └── product.ts             # TypeScript типи
├── lib/
│   └── api.ts                 # Функції для отримання даних (Server-side)
└── components/
    ├── ProductList.tsx        # Server Component - отримує дані
    ├── ProductCard.tsx        # Client Component - інтерактивна картка
    └── AddToCartButton.tsx    # Client Component - кнопка з логікою
```

### Компоненти та їх призначення

#### Server Components

**1. `app/page.tsx`**
- **Тип:** Server Component
- **Призначення:** Головна сторінка, яка організовує структуру та використовує Suspense для loading стану
- **Чому Server Component:**
  - Не потребує інтерактивності на рівні сторінки
  - Використовує Suspense для оптимізації завантаження
  - Рендериться на сервері для кращого SEO

**2. `app/components/ProductList.tsx`**
- **Тип:** Server Component
- **Призначення:** Отримує дані про продукти з API та рендерить список
- **Чому Server Component:**
  - Використовує `async/await` для отримання даних
  - Не потребує інтерактивності
  - Виконується на сервері, що покращує швидкість завантаження
  - Зменшує розмір JavaScript bundle на клієнті
  - Може безпосередньо працювати з базами даних та API

#### Client Components

**3. `app/components/ProductCard.tsx`**
- **Тип:** Client Component (`'use client'`)
- **Призначення:** Інтерактивна картка продукту з можливістю додавання до обраних
- **Чому Client Component:**
  - Використовує React hooks (`useState`, `useEffect`)
  - Обробляє події користувача (клік по кнопці обраного)
  - Має доступ до браузерних API (`localStorage`)
  - Показує динамічний контент на основі стану

**4. `app/components/AddToCartButton.tsx`**
- **Тип:** Client Component (`'use client'`)
- **Призначення:** Кнопка додавання до кошика з інтерактивним селектором кількості
- **Чому Client Component:**
  - Потребує інтерактивності (кнопки, обробка кліків)
  - Використовує `useState` для управління станом (loading, success)
  - Має доступ до `localStorage` для збереження кошика
  - Показує динамічний UI на основі стану

### Потік даних

```
Server Component (page.tsx)
    ↓
Server Component (ProductList.tsx) - отримує дані з API
    ↓ передає дані через props
Client Component (ProductCard.tsx) - відображає та обробляє інтерактивність
    ↓ передає дані через props
Client Component (AddToCartButton.tsx) - обробляє додавання до кошика
```

## 🔑 Ключові концепції

### Server Components

**Переваги:**
- ✅ Виконуються на сервері, зменшуючи розмір JavaScript bundle
- ✅ Можуть безпосередньо працювати з базами даних та API
- ✅ Покращують SEO завдяки серверному рендерингу
- ✅ Швидше завантаження для користувача
- ✅ Можуть використовувати `async/await` безпосередньо

**Обмеження:**
- ❌ Не можуть використовувати React hooks
- ❌ Не можуть обробляти події користувача
- ❌ Не мають доступу до браузерних API

### Client Components

**Переваги:**
- ✅ Можуть використовувати React hooks
- ✅ Можуть обробляти події користувача
- ✅ Мають доступ до браузерних API
- ✅ Підтримують динамічний контент

**Обмеження:**
- ❌ Збільшують розмір JavaScript bundle
- ❌ Виконуються на клієнті, що може уповільнити завантаження
- ❌ Не можуть безпосередньо працювати з базами даних

### Правила інтеграції

1. **Server Components можуть імпортувати та використовувати Client Components**
2. **Client Components НЕ можуть імпортувати Server Components**
3. **Дані передаються від Server до Client Components через props**
4. **Використовуйте "use client" directive тільки коли це необхідно**

## 🛠️ Технології

- **Next.js 14+** - фреймворк з App Router
- **React 18** - бібліотека для UI
- **TypeScript 5+** - типізація коду
- **Tailwind CSS** - стилізація

## 🚀 Запуск проєкту

### Встановлення залежностей

```bash
npm install
```

### Запуск в режимі розробки

```bash
npm run dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

### Збірка для продакшну

```bash
npm run build
npm start
```

## 📝 Особливості реалізації

### 1. Обробка помилок

- **Server Component (`ProductList.tsx`):** Використовує try-catch для обробки помилок при отриманні даних
- **Client Components:** Показують відповідні повідомлення при помилках

### 2. Loading States

- **Suspense:** Використовується на рівні сторінки для показу loading стану під час завантаження даних
- **Локальні стани:** Client Components використовують `useState` для показу loading стану під час асинхронних операцій

### 3. TypeScript типізація

- Всі компоненти мають повну типізацію
- Використовуються інтерфейси для типізації даних (`Product`, `CartItem`)
- Props компонентів типізовані через TypeScript interfaces

### 4. Стилізація

- Використовується Tailwind CSS для стилізації
- Підтримка темної теми через `dark:` префікси
- Адаптивний дизайн для різних розмірів екранів

## 🎯 Демонстрація функціоналу

### Server Component функціонал:
- ✅ Отримання даних з API з використанням `async/await`
- ✅ Серверний рендеринг статичного контенту
- ✅ Обробка помилок на сервері

### Client Component функціонал:
- ✅ Використання React hooks (`useState`, `useEffect`)
- ✅ Обробка подій користувача (кліки, зміна кількості)
- ✅ Доступ до браузерних API (`localStorage`)
- ✅ Динамічний UI на основі стану (loading, success)

### Інтеграція:
- ✅ Server Component передає дані до Client Component через props
- ✅ Чітке розмежування між Server та Client Components
- ✅ Правильне використання "use client" directive

## 📚 Додаткові матеріали

- [Next.js Server Components Documentation](https://nextjs.org/docs/app/building-your-application/rendering/server-components)
- [React Server Components](https://react.dev/blog/2023/03/22/react-labs-what-we-have-been-working-on-march-2023#react-server-components)
- [Next.js App Router](https://nextjs.org/docs/app)

## 👤 Автор

Проєкт створено для демонстрації технічних навичок роботи з Next.js 14+ та Server/Client Components.

---

**Час виконання:** 2-4 години  
**Версія Next.js:** 14.2.5  
**Версія TypeScript:** 5.5.4

